// Online C compiler to run C program online
#include <stdio.h>

void fib(int n)
{
    static int a=0,b=1,c;
    if(n>0)
        {
            c=a+b;
            a=b;
            b=c;
            printf(" %d",c);
            fib(n-1);
            
        }
}
void main() {
    int n;
    printf("Enter a limit: ");
    scanf("%d",&n);
    if(n==0 || n==1)
    {
        printf("0");
    }
    else if(n==2)
    {
        printf("0 1");
    }
    else 
        printf("0 1");
        fib(n-2);
        
}